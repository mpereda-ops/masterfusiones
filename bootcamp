<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Design Thinking Bootcamp — SimulabAI | Universidad de Salamanca</title>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700;800;900&family=Courier+Prime:wght@400;700&display=swap" rel="stylesheet">
<style>
:root {
  --orange: #ff6b35;
  --orange-light: #ff8c61;
  --orange-dark: #e85a28;
  --black: #1a1a1a;
  --white: #ffffff;
  --gray-light: #f5f5f5;
  --gray: #9e9e9e;
  --gray-dark: #424242;
  --yellow: #ffd93d;
  --green: #6bcf7f;
  --blue: #4ecdc4;
  --purple: #a55eea;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: 'Poppins', sans-serif;
  background: var(--white);
  color: var(--black);
  min-height: 100vh;
  overflow-x: hidden;
  font-size: 16px;
  line-height: 1.6;
}

/* ══════════════════════════════════════════════════════════════════
   INTRO SCREEN
   ══════════════════════════════════════════════════════════════════ */

#screen-intro {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: linear-gradient(135deg, var(--orange) 0%, var(--orange-dark) 100%);
  padding: 40px 20px;
}

.intro-card {
  max-width: 800px;
  background: white;
  border-radius: 24px;
  padding: 60px;
  box-shadow: 0 30px 90px rgba(0,0,0,0.3);
  animation: slideIn 0.6s ease-out;
}

@keyframes slideIn {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}

.intro-badge {
  display: inline-block;
  background: var(--orange);
  color: white;
  padding: 8px 20px;
  border-radius: 20px;
  font-size: 12px;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  margin-bottom: 20px;
}

.intro-title {
  font-size: 48px;
  font-weight: 900;
  line-height: 1.1;
  margin-bottom: 20px;
  color: var(--black);
}

.intro-subtitle {
  font-size: 20px;
  color: var(--gray-dark);
  margin-bottom: 30px;
  line-height: 1.6;
}

.intro-phases {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 12px;
  margin: 40px 0;
}

.intro-phase {
  text-align: center;
  padding: 16px 8px;
  background: var(--gray-light);
  border-radius: 12px;
  font-size: 13px;
  font-weight: 600;
}

.intro-phase-number {
  display: block;
  font-size: 24px;
  color: var(--orange);
  margin-bottom: 6px;
}

.case-selector {
  margin: 30px 0;
  padding: 20px;
  background: var(--gray-light);
  border-radius: 12px;
}

.case-selector h3 {
  font-size: 16px;
  margin-bottom: 16px;
  color: var(--black);
}

.case-options {
  display: grid;
  gap: 12px;
}

.case-option {
  padding: 16px;
  background: white;
  border: 2px solid transparent;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.2s;
  font-size: 14px;
}

.case-option:hover {
  border-color: var(--orange);
  transform: translateX(4px);
}

.case-option.selected {
  border-color: var(--orange);
  background: rgba(255,107,53,0.1);
}

.case-option strong {
  display: block;
  margin-bottom: 4px;
  color: var(--orange);
}

.btn-start {
  width: 100%;
  background: linear-gradient(135deg, var(--orange) 0%, var(--orange-dark) 100%);
  color: white;
  border: none;
  padding: 20px;
  border-radius: 12px;
  font-size: 18px;
  font-weight: 800;
  cursor: pointer;
  transition: all 0.3s;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-top: 30px;
}

.btn-start:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 12px 40px rgba(255,107,53,0.4);
}

.btn-start:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.intro-footer {
  margin-top: 30px;
  padding-top: 20px;
  border-top: 1px solid var(--gray);
  font-size: 13px;
  color: var(--gray);
  text-align: center;
}

/* ══════════════════════════════════════════════════════════════════
   MAIN SIMULATOR
   ══════════════════════════════════════════════════════════════════ */

#screen-main {
  display: none;
  min-height: 100vh;
  background: var(--gray-light);
}

#screen-main.active {
  display: block;
}

.main-header {
  background: white;
  border-bottom: 3px solid var(--orange);
  padding: 20px 30px;
  position: sticky;
  top: 0;
  z-index: 100;
  box-shadow: 0 2px 20px rgba(0,0,0,0.1);
}

.main-header-content {
  max-width: 1400px;
  margin: 0 auto;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.header-title {
  font-size: 22px;
  font-weight: 800;
  color: var(--black);
}

.header-meta {
  display: flex;
  gap: 30px;
  align-items: center;
  font-size: 14px;
  font-weight: 600;
  color: var(--gray);
}

.header-meta strong {
  color: var(--orange);
  margin-left: 6px;
}

.progress-bar {
  max-width: 1400px;
  margin: 0 auto;
  padding: 30px;
}

.progress-steps {
  display: flex;
  justify-content: space-between;
  position: relative;
}

.progress-steps::before {
  content: '';
  position: absolute;
  top: 20px;
  left: 40px;
  right: 40px;
  height: 4px;
  background: var(--gray);
  z-index: 0;
}

.progress-line {
  position: absolute;
  top: 20px;
  left: 40px;
  height: 4px;
  background: var(--orange);
  z-index: 1;
  transition: width 0.4s;
}

.progress-step {
  position: relative;
  z-index: 2;
  text-align: center;
  flex: 1;
}

.progress-circle {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: white;
  border: 4px solid var(--gray);
  margin: 0 auto 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 800;
  font-size: 16px;
  color: var(--gray);
  transition: all 0.3s;
}

.progress-step.active .progress-circle {
  border-color: var(--orange);
  background: var(--orange);
  color: white;
  box-shadow: 0 4px 20px rgba(255,107,53,0.4);
}

.progress-step.completed .progress-circle {
  border-color: var(--green);
  background: var(--green);
  color: white;
}

.progress-step-name {
  font-size: 13px;
  font-weight: 600;
  color: var(--gray-dark);
}

.progress-step.active .progress-step-name {
  color: var(--orange);
  font-weight: 800;
}

.canvas-content {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 30px 60px;
}

.phase-card {
  background: white;
  border-radius: 20px;
  padding: 50px;
  box-shadow: 0 10px 40px rgba(0,0,0,0.08);
  animation: fadeIn 0.4s;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.phase-header {
  margin-bottom: 40px;
  padding-bottom: 30px;
  border-bottom: 3px solid var(--gray-light);
}

.phase-number {
  font-size: 14px;
  font-weight: 800;
  color: var(--orange);
  text-transform: uppercase;
  letter-spacing: 0.1em;
  margin-bottom: 12px;
}

.phase-title {
  font-size: 36px;
  font-weight: 900;
  color: var(--black);
  margin-bottom: 16px;
  line-height: 1.2;
}

.phase-description {
  font-size: 18px;
  color: var(--gray-dark);
  line-height: 1.7;
}

.tool-section {
  margin: 40px 0;
}

.tool-title {
  font-size: 20px;
  font-weight: 800;
  color: var(--black);
  margin-bottom: 20px;
  display: flex;
  align-items: center;
  gap: 12px;
}

.tool-icon {
  width: 32px;
  height: 32px;
  background: var(--orange);
  color: white;
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18px;
}

.canvas-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20px;
  margin: 20px 0;
}

.canvas-box {
  background: var(--gray-light);
  border-radius: 12px;
  padding: 20px;
  min-height: 150px;
}

.canvas-box-title {
  font-size: 14px;
  font-weight: 700;
  color: var(--orange);
  margin-bottom: 12px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.canvas-textarea {
  width: 100%;
  min-height: 120px;
  padding: 12px;
  border: 2px solid var(--gray);
  border-radius: 8px;
  font-family: 'Poppins', sans-serif;
  font-size: 15px;
  resize: vertical;
  transition: all 0.2s;
}

.canvas-textarea:focus {
  outline: none;
  border-color: var(--orange);
  box-shadow: 0 0 0 4px rgba(255,107,53,0.1);
}

.input-group {
  margin: 24px 0;
}

.input-label {
  font-size: 16px;
  font-weight: 700;
  color: var(--black);
  margin-bottom: 10px;
  display: block;
}

.input-field {
  width: 100%;
  padding: 14px 18px;
  border: 2px solid var(--gray);
  border-radius: 8px;
  font-size: 15px;
  font-family: 'Poppins', sans-serif;
  transition: all 0.2s;
}

.input-field:focus {
  outline: none;
  border-color: var(--orange);
  box-shadow: 0 0 0 4px rgba(255,107,53,0.1);
}

.btn-primary {
  background: linear-gradient(135deg, var(--orange) 0%, var(--orange-dark) 100%);
  color: white;
  border: none;
  padding: 16px 40px;
  border-radius: 10px;
  font-size: 16px;
  font-weight: 800;
  cursor: pointer;
  transition: all 0.3s;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

.btn-primary:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 8px 30px rgba(255,107,53,0.3);
}

.btn-primary:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.btn-secondary {
  background: white;
  color: var(--orange);
  border: 2px solid var(--orange);
  padding: 14px 32px;
  border-radius: 10px;
  font-size: 15px;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.2s;
  margin-left: 12px;
}

.btn-secondary:hover {
  background: var(--orange);
  color: white;
}

.ai-response {
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
  border-left: 5px solid var(--blue);
  padding: 30px;
  border-radius: 12px;
  margin: 30px 0;
  font-family: 'Courier Prime', monospace;
  font-size: 15px;
  line-height: 1.8;
  white-space: pre-wrap;
}

.ai-response h4 {
  font-family: 'Poppins', sans-serif;
  font-size: 18px;
  color: var(--blue);
  margin-bottom: 16px;
}

.thinking {
  text-align: center;
  padding: 60px 20px;
  opacity: 0;
  transition: opacity 0.3s;
  pointer-events: none;
}

.thinking.visible {
  opacity: 1;
}

.thinking-spinner {
  width: 50px;
  height: 50px;
  border: 4px solid var(--gray-light);
  border-top: 4px solid var(--orange);
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin: 0 auto 20px;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.thinking-text {
  font-size: 16px;
  color: var(--gray);
  font-weight: 600;
}

.score-badge {
  display: inline-block;
  padding: 6px 16px;
  border-radius: 20px;
  font-size: 14px;
  font-weight: 800;
  margin-left: 12px;
}

.score-high {
  background: var(--green);
  color: white;
}

.score-medium {
  background: var(--yellow);
  color: var(--black);
}

.score-low {
  background: var(--orange);
  color: white;
}

.info-box {
  background: rgba(78,205,196,0.1);
  border-left: 4px solid var(--blue);
  padding: 20px;
  border-radius: 8px;
  margin: 20px 0;
  font-size: 15px;
  line-height: 1.7;
}

.info-box strong {
  color: var(--blue);
}

.crazy8-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
  margin: 30px 0;
}

.crazy8-cell {
  aspect-ratio: 1;
  background: white;
  border: 3px dashed var(--gray);
  border-radius: 12px;
  padding: 16px;
  position: relative;
}

.crazy8-cell textarea {
  width: 100%;
  height: 100%;
  border: none;
  font-size: 14px;
  resize: none;
  font-family: 'Poppins', sans-serif;
}

.crazy8-cell textarea:focus {
  outline: none;
}

.crazy8-number {
  position: absolute;
  top: 8px;
  left: 8px;
  width: 28px;
  height: 28px;
  background: var(--orange);
  color: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 800;
  font-size: 14px;
}

.timer-display {
  text-align: center;
  font-size: 48px;
  font-weight: 900;
  color: var(--orange);
  margin: 30px 0;
  font-family: 'Courier Prime', monospace;
}

.idea-card {
  background: white;
  border: 2px solid var(--gray);
  border-radius: 12px;
  padding: 20px;
  margin: 16px 0;
  position: relative;
  cursor: pointer;
  transition: all 0.2s;
}

.idea-card:hover {
  border-color: var(--orange);
  transform: translateX(4px);
}

.idea-card.selected {
  border-color: var(--orange);
  border-width: 3px;
  background: rgba(255,107,53,0.05);
}

.idea-vote {
  position: absolute;
  top: 20px;
  right: 20px;
  width: 40px;
  height: 40px;
  border: 3px solid var(--gray);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
  transition: all 0.2s;
}

.idea-card.selected .idea-vote {
  border-color: var(--orange);
  background: var(--orange);
}

.storyboard-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
  margin: 30px 0;
}

.storyboard-frame {
  background: white;
  border: 3px solid var(--gray);
  border-radius: 12px;
  padding: 16px;
  aspect-ratio: 4/3;
}

.storyboard-frame textarea {
  width: 100%;
  height: 100%;
  border: none;
  font-size: 14px;
  resize: none;
  font-family: 'Poppins', sans-serif;
}

.storyboard-label {
  font-size: 12px;
  font-weight: 700;
  color: var(--orange);
  margin-bottom: 8px;
  text-transform: uppercase;
}

.final-dashboard {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
  margin: 40px 0;
}

.metric-card {
  background: linear-gradient(135deg, var(--orange) 0%, var(--orange-dark) 100%);
  color: white;
  padding: 30px;
  border-radius: 16px;
  text-align: center;
}

.metric-value {
  font-size: 48px;
  font-weight: 900;
  margin-bottom: 8px;
}

.metric-label {
  font-size: 14px;
  font-weight: 600;
  opacity: 0.9;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.hidden { display: none !important; }

@media (max-width: 768px) {
  .intro-phases { grid-template-columns: 1fr; }
  .canvas-grid { grid-template-columns: 1fr; }
  .crazy8-grid { grid-template-columns: repeat(2, 1fr); }
  .storyboard-grid { grid-template-columns: 1fr; }
  .final-dashboard { grid-template-columns: 1fr; }
}
</style>
</head>
<body>

<div id="app">
  <!-- INTRO -->
  <div id="screen-intro">
    <div class="intro-card">
      <div class="intro-badge">Universidad de Salamanca · SimulabAI</div>
      <h1 class="intro-title">Design Thinking Bootcamp</h1>
      <p class="intro-subtitle">Completa el ciclo de innovación usando las técnicas del Design Thinking. La IA te guiará, validará y desafiará en cada fase.</p>
      
      <div class="intro-phases">
        <div class="intro-phase">
          <span class="intro-phase-number">1</span>
          Empatizar
        </div>
        <div class="intro-phase">
          <span class="intro-phase-number">2</span>
          Definir
        </div>
        <div class="intro-phase">
          <span class="intro-phase-number">3</span>
          Idear
        </div>
        <div class="intro-phase">
          <span class="intro-phase-number">4</span>
          Prototipar
        </div>
        <div class="intro-phase">
          <span class="intro-phase-number">5</span>
          Testear
        </div>
      </div>
      
      <div class="case-selector">
        <h3>🎯 Elige tu reto de innovación:</h3>
        <div class="case-options">
          <div class="case-option" data-case="cafeteria">
            <strong>☕ Cafetería Universitaria</strong>
            Mejorar la experiencia de estudiantes en la cafetería del campus
          </div>
          <div class="case-option" data-case="movilidad">
            <strong>🚴 Movilidad Sostenible</strong>
            App para promover transporte sostenible en ciudades medianas
          </div>
          <div class="case-option" data-case="mentoria">
            <strong>💡 Plataforma de Mentorización</strong>
            Conectar estudiantes con profesionales de su sector
          </div>
          <div class="case-option" data-case="salud-mental">
            <strong>🧠 Salud Mental Estudiantil</strong>
            Servicio de apoyo psicológico accesible para estudiantes
          </div>
          <div class="case-option" data-case="desperdicio">
            <strong>🍽️ Desperdicio Alimentario</strong>
            Reducir el desperdicio de comida en restaurantes universitarios
          </div>
        </div>
      </div>
      
      <button class="btn-start" id="btn-start" disabled>Comenzar Bootcamp →</button>
      
      <div class="intro-footer">
        Diseñado por Prof. Jorge Matías-Pereda · Powered by Claude Sonnet 4
      </div>
    </div>
  </div>

  <!-- MAIN SIMULATOR -->
  <div id="screen-main">
    <div class="main-header">
      <div class="main-header-content">
        <div class="header-title">Design Thinking Bootcamp</div>
        <div class="header-meta">
          <span>TIEMPO<strong id="timer">00:00</strong></span>
          <span>RETO<strong id="case-name">-</strong></span>
        </div>
      </div>
    </div>

    <div class="progress-bar">
      <div class="progress-steps">
        <div class="progress-line" id="progress-line"></div>
        <div class="progress-step active" id="step-1">
          <div class="progress-circle">1</div>
          <div class="progress-step-name">Empatizar</div>
        </div>
        <div class="progress-step" id="step-2">
          <div class="progress-circle">2</div>
          <div class="progress-step-name">Definir</div>
        </div>
        <div class="progress-step" id="step-3">
          <div class="progress-circle">3</div>
          <div class="progress-step-name">Idear</div>
        </div>
        <div class="progress-step" id="step-4">
          <div class="progress-circle">4</div>
          <div class="progress-step-name">Prototipar</div>
        </div>
        <div class="progress-step" id="step-5">
          <div class="progress-circle">5</div>
          <div class="progress-step-name">Testear</div>
        </div>
      </div>
    </div>

    <div class="canvas-content" id="canvas-content"></div>
  </div>
</div>

<script>
// SCORM DRIVER (inline)
const SCORM = {
  initialized: false,
  init() {
    try {
      const API = this.findAPI(window);
      if (API) { API.Initialize(''); this.initialized = true; console.log('SCORM initialized'); }
    } catch (e) { console.log('SCORM not available'); }
    return this.initialized;
  },
  findAPI(win) {
    let attempts = 0;
    while (win && attempts < 7) {
      if (win.API_1484_11) return win.API_1484_11;
      if (win.API) return win.API;
      if (win.parent && win.parent !== win) { attempts++; win = win.parent; } else { break; }
    }
    return null;
  },
  setValue(key, value) {
    if (!this.initialized) return;
    try { const API = this.findAPI(window); if (API) API.SetValue(key, value); } catch (e) {}
  },
  setScore(score) { this.setValue('cmi.score.raw', score); this.setValue('cmi.score.min', '0'); this.setValue('cmi.score.max', '100'); },
  setCompleted() { this.setValue('cmi.completion_status', 'completed'); this.setValue('cmi.success_status', 'passed'); },
  commit() { try { const API = this.findAPI(window); if (API) API.Commit(''); } catch (e) {} },
  finish() { try { const API = this.findAPI(window); if (API) { this.commit(); API.Terminate(''); } } catch (e) {} }
};

// STATE
const state = {
  phase: 0,
  case: null,
  sessionStart: null,
  timerInterval: null,
  data: {
    // Fase 1
    empathyMap: { thinks: '', feels: '', sees: '', hears: '', says: '', does: '' },
    interviewQA: [],
    painPoints: [],
    // Fase 2
    povStatement: '',
    hmwQuestions: [],
    selectedHMW: [],
    // Fase 3
    crazy8Ideas: [],
    scamperIdeas: [],
    selectedIdeas: [],
    // Fase 4
    storyboard: ['','','','','',''],
    prototypeDesc: '',
    valueProposition: '',
    // Fase 5
    feedbackResults: '',
    decision: '',
    iterationPlan: ''
  },
  scores: {
    empathy: 0,
    creativity: 0,
    viability: 0,
    prototype: 0,
    overall: 0
  }
};



const CASES = {
  'cafeteria': {
    name: 'Cafetería Universitaria',
    context: 'Mejorar la experiencia de estudiantes en la cafetería del campus',
    persona: 'Estudiante universitario de 20 años que usa la cafetería diariamente'
  },
  'movilidad': {
    name: 'Movilidad Sostenible',
    context: 'App para promover transporte sostenible en ciudades medianas',
    persona: 'Profesional joven de 28 años que se desplaza 15km diarios al trabajo'
  },
  'mentoria': {
    name: 'Plataforma de Mentorización',
    context: 'Conectar estudiantes con profesionales de su sector',
    persona: 'Estudiante de último año buscando orientación profesional'
  },
  'salud-mental': {
    name: 'Salud Mental Estudiantil',
    context: 'Servicio de apoyo psicológico accesible para estudiantes',
    persona: 'Estudiante de 22 años con ansiedad y estrés por exámenes'
  },
  'desperdicio': {
    name: 'Desperdicio Alimentario',
    context: 'Reducir el desperdicio de comida en restaurantes universitarios',
    persona: 'Responsable de cocina en comedor universitario'
  }
};

// INIT
document.addEventListener('DOMContentLoaded', () => {
  // Case selector
  document.querySelectorAll('.case-option').forEach(opt => {
    opt.addEventListener('click', () => {
      document.querySelectorAll('.case-option').forEach(o => o.classList.remove('selected'));
      opt.classList.add('selected');
      state.case = opt.dataset.case;
      document.getElementById('btn-start').disabled = false;
    });
  });

  document.getElementById('btn-start').addEventListener('click', iniciarBootcamp);
});

function iniciarBootcamp() {
  if (!state.case) return;
  SCORM.init();
  state.sessionStart = new Date();
  state.timerInterval = setInterval(updateTimer, 1000);
  
  document.getElementById('screen-intro').style.display = 'none';
  document.getElementById('screen-main').classList.add('active');
  document.getElementById('case-name').textContent = CASES[state.case].name;
  
  renderPhase(1);
}

function updateTimer() {
  const elapsed = Math.floor((new Date() - state.sessionStart) / 1000);
  const m = Math.floor(elapsed / 60).toString().padStart(2, '0');
  const s = (elapsed % 60).toString().padStart(2, '0');
  document.getElementById('timer').textContent = `${m}:${s}`;
}

function updateProgress(phase) {
  state.phase = phase;
  
  // Update steps
  for (let i = 1; i <= 5; i++) {
    const step = document.getElementById(`step-${i}`);
    step.classList.remove('active', 'completed');
    if (i < phase) step.classList.add('completed');
    if (i === phase) step.classList.add('active');
  }
  
  // Update progress line
  const width = ((phase - 1) / 4) * 100;
  document.getElementById('progress-line').style.width = `calc(${width}% - 80px)`;
}

async function llamarClaude(prompt) {
  const response = await fetch('https://broken-flower-ecde.mpereda.workers.dev/v1/messages', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      model: 'claude-sonnet-4-20250514',
      max_tokens: 3000,
      messages: [{ role: 'user', content: prompt }]
    })
  });
  
  if (!response.ok) throw new Error('API Error');
  const data = await response.json();
  return data.content[0].text;
}

// ══════════════════════════════════════════════════════════════════
// RENDER PHASES
// ══════════════════════════════════════════════════════════════════

function renderPhase(phase) {
  updateProgress(phase);
  const canvas = document.getElementById('canvas-content');
  
  if (phase === 1) renderFase1(canvas);
  else if (phase === 2) renderFase2(canvas);
  else if (phase === 3) renderFase3(canvas);
  else if (phase === 4) renderFase4(canvas);
  else if (phase === 5) renderFase5(canvas);
}

// FASE 1: EMPATIZAR
function renderFase1(canvas) {
  canvas.innerHTML = `
    <div class="phase-card">
      <div class="phase-header">
        <div class="phase-number">◆ Fase 1 de 5</div>
        <h2 class="phase-title">Empatizar</h2>
        <p class="phase-description">Entiende profundamente a tu usuario. No asumas, pregunta y observa.</p>
      </div>

      <div class="info-box">
        <strong>Tu reto:</strong> ${CASES[state.case].context}<br>
        <strong>Tu persona objetivo:</strong> ${CASES[state.case].persona}
      </div>

      <div class="tool-section">
        <div class="tool-title">
          <div class="tool-icon">📝</div>
          1. Mapa de Empatía
        </div>
        <p style="margin-bottom: 20px; color: var(--gray-dark);">Completa cada cuadrante pensando en tu persona objetivo.</p>
        <div class="canvas-grid">
          <div class="canvas-box">
            <div class="canvas-box-title">¿Qué PIENSA y SIENTE?</div>
            <textarea class="canvas-textarea" id="empathy-thinks" placeholder="Sus preocupaciones, aspiraciones, emociones..."></textarea>
          </div>
          <div class="canvas-box">
            <div class="canvas-box-title">¿Qué VE?</div>
            <textarea class="canvas-textarea" id="empathy-sees" placeholder="Entorno, amigos, mercado..."></textarea>
          </div>
          <div class="canvas-box">
            <div class="canvas-box-title">¿Qué OYE?</div>
            <textarea class="canvas-textarea" id="empathy-hears" placeholder="Lo que le dicen amigos, medios, influencers..."></textarea>
          </div>
          <div class="canvas-box">
            <div class="canvas-box-title">¿Qué DICE y HACE?</div>
            <textarea class="canvas-textarea" id="empathy-says" placeholder="Comportamientos, actitudes públicas..."></textarea>
          </div>
        </div>
      </div>

      <div class="tool-section">
        <div class="tool-title">
          <div class="tool-icon">💬</div>
          2. Entrevista Simulada con IA
        </div>
        <p style="margin-bottom: 20px; color: var(--gray-dark);">Haz 5 preguntas al usuario. La IA responderá como si fuera esa persona.</p>
        
        <div id="interview-container">
          <div class="input-group">
            <label class="input-label">Pregunta 1:</label>
            <input type="text" class="input-field interview-question" placeholder="¿Qué es lo más frustrante de...?">
          </div>
        </div>
        
        <button class="btn-secondary" onclick="addInterviewQuestion()">+ Añadir pregunta</button>
        <button class="btn-primary" onclick="conductInterview()">Realizar Entrevista</button>
      </div>

      <div id="interview-results" class="hidden"></div>

      <div id="thinking-1" class="thinking hidden"></div>

      <div style="margin-top: 50px; text-align: right;">
        <button class="btn-primary" onclick="completarFase1()">Continuar a Fase 2: Definir →</button>
      </div>
    </div>
  `;
}

let questionCount = 1;
function addInterviewQuestion() {
  if (questionCount >= 7) return;
  questionCount++;
  const container = document.getElementById('interview-container');
  const div = document.createElement('div');
  div.className = 'input-group';
  div.innerHTML = `
    <label class="input-label">Pregunta ${questionCount}:</label>
    <input type="text" class="input-field interview-question" placeholder="Otra pregunta...">
  `;
  container.appendChild(div);
}

async function conductInterview() {
  const questions = Array.from(document.querySelectorAll('.interview-question'))
    .map(q => q.value.trim())
    .filter(q => q);
  
  if (questions.length === 0) {
    alert('Escribe al menos una pregunta');
    return;
  }

  showThinking('thinking-1', 'La IA está respondiendo como usuario...');

  const prompt = `Eres un usuario real: ${CASES[state.case].persona}. 
Contexto: ${CASES[state.case].context}.

El diseñador te hace estas preguntas. Responde como si fueras esa persona real, con emociones, frustraciones y experiencias concretas. Sé específico.

Preguntas:
${questions.map((q, i) => `${i + 1}. ${q}`).join('\n')}

Responde en formato:
P1: [pregunta]
R1: [tu respuesta realista]
...`;

  try {
    const result = await llamarClaude(prompt);
    state.data.interviewQA = result;
    
    document.getElementById('interview-results').innerHTML = `
      <div class="ai-response">
        <h4>🎤 Respuestas del Usuario:</h4>
        ${result}
      </div>
    `;
    document.getElementById('interview-results').classList.remove('hidden');
    hideThinking('thinking-1');
  } catch (error) {
    hideThinking('thinking-1');
    alert('Error en la entrevista: ' + error.message);
  }
}

async function completarFase1() {
  // Guardar mapa de empatía
  state.data.empathyMap = {
    thinks: document.getElementById('empathy-thinks').value,
    sees: document.getElementById('empathy-sees').value,
    hears: document.getElementById('empathy-hears').value,
    says: document.getElementById('empathy-says').value
  };

  // Validar que tenga algo
  if (!state.data.empathyMap.thinks || !state.data.interviewQA) {
    alert('Completa el mapa de empatía y la entrevista antes de continuar');
    return;
  }

  // Guardar en SCORM
  SCORM.setValue('cmi.suspend_data', JSON.stringify(state));
  
  renderPhase(2);
}

// FASE 2: DEFINIR
function renderFase2(canvas) {
  canvas.innerHTML = `
    <div class="phase-card">
      <div class="phase-header">
        <div class="phase-number">◆ Fase 2 de 5</div>
        <h2 class="phase-title">Definir</h2>
        <p class="phase-description">Convierte tus hallazgos en un problema claro y accionable.</p>
      </div>

      <div class="tool-section">
        <div class="tool-title">
          <div class="tool-icon">🎯</div>
          1. Point of View (POV) Statement
        </div>
        <p style="margin-bottom: 20px; color: var(--gray-dark);">Define el problema desde la perspectiva del usuario.</p>
        
        <div class="info-box">
          <strong>Fórmula:</strong> [Usuario] necesita [necesidad] porque [insight sorprendente]
        </div>

        <div class="input-group">
          <label class="input-label">Tu POV Statement:</label>
          <textarea id="pov-statement" class="input-field" style="min-height: 100px;" placeholder="Ejemplo: Los estudiantes universitarios necesitan una forma rápida de comer saludable entre clases porque el estrés y la falta de tiempo les lleva a opciones poco nutritivas que afectan su rendimiento académico"></textarea>
        </div>

        <button class="btn-primary" onclick="validarPOV()">Validar POV con IA</button>
      </div>

      <div id="pov-validation" class="hidden"></div>

      <div class="tool-section" id="hmw-section" style="display: none;">
        <div class="tool-title">
          <div class="tool-icon">💡</div>
          2. "How Might We" (HMW) Questions
        </div>
        <p style="margin-bottom: 20px; color: var(--gray-dark);">Convierte tu POV en preguntas abiertas que inviten a soluciones creativas.</p>

        <div id="hmw-container"></div>
      </div>

      <div id="thinking-2" class="thinking hidden"></div>

      <div style="margin-top: 50px; text-align: right;">
        <button class="btn-secondary" onclick="renderPhase(1)">← Volver a Fase 1</button>
        <button class="btn-primary" id="btn-fase2" onclick="completarFase2()" disabled>Continuar a Fase 3: Idear →</button>
      </div>
    </div>
  `;
}

async function validarPOV() {
  const pov = document.getElementById('pov-statement').value.trim();
  if (!pov) {
    alert('Escribe tu POV Statement primero');
    return;
  }

  showThinking('thinking-2', 'La IA está validando tu POV...');

  const prompt = `Evalúa este POV Statement de Design Thinking:

"${pov}"

Contexto del reto: ${CASES[state.case].context}

Evalúa:
1. ¿Es específico? (no genérico)
2. ¿Es accionable? (sugiere direcciones de solución)
3. ¿Está centrado en el usuario? (no en tecnología o negocio)
4. ¿El insight es profundo? (no obvio)

Da feedback constructivo y una puntuación de 0-100. Si es bajo, sugiere cómo mejorarlo.

Luego genera 5 preguntas "How Might We" (HMW) derivadas de este POV. Que sean creativas y desde ángulos diferentes.

Formato:
PUNTUACIÓN: X/100
FEEDBACK: [tu análisis]

HMW QUESTIONS:
1. ¿Cómo podríamos...?
2. ¿Cómo podríamos...?
...`;

  try {
    const result = await llamarClaude(prompt);
    state.data.povStatement = pov;
    
    // Extraer puntuación
    const scoreMatch = result.match(/PUNTUACIÓN:\s*(\d+)/);
    const score = scoreMatch ? parseInt(scoreMatch[1]) : 50;
    
    const badgeClass = score >= 75 ? 'score-high' : score >= 50 ? 'score-medium' : 'score-low';
    
    document.getElementById('pov-validation').innerHTML = `
      <div class="ai-response">
        <h4>📊 Evaluación de tu POV <span class="score-badge ${badgeClass}">${score}/100</span></h4>
        ${result}
      </div>
    `;
    document.getElementById('pov-validation').classList.remove('hidden');
    
    // Mostrar sección HMW
    document.getElementById('hmw-section').style.display = 'block';
    
    // Extraer HMW questions del resultado
    const hmwMatch = result.match(/HMW QUESTIONS:(.*)/s);
    if (hmwMatch) {
      const hmwText = hmwMatch[1];
      const hmws = hmwText.split('\n').filter(line => line.trim().match(/^\d+\./)).map(line => line.replace(/^\d+\.\s*/, '').trim());
      
      document.getElementById('hmw-container').innerHTML = hmws.map((hmw, i) => `
        <div class="idea-card" onclick="toggleHMW(${i})">
          <div class="idea-vote">○</div>
          <strong>HMW ${i + 1}:</strong><br>${hmw}
        </div>
      `).join('');
      
      state.data.hmwQuestions = hmws;
    }
    
    document.getElementById('btn-fase2').disabled = false;
    hideThinking('thinking-2');
  } catch (error) {
    hideThinking('thinking-2');
    alert('Error: ' + error.message);
  }
}

function toggleHMW(index) {
  const cards = document.querySelectorAll('#hmw-container .idea-card');
  const card = cards[index];
  const wasSelected = card.classList.contains('selected');
  
  if (wasSelected) {
    card.classList.remove('selected');
    card.querySelector('.idea-vote').textContent = '○';
    state.data.selectedHMW = state.data.selectedHMW.filter(i => i !== index);
  } else {
    if (state.data.selectedHMW.length >= 3) {
      alert('Máximo 3 HMW seleccionados');
      return;
    }
    card.classList.add('selected');
    card.querySelector('.idea-vote').textContent = '●';
    state.data.selectedHMW.push(index);
  }
}

function completarFase2() {
  if (state.data.selectedHMW.length === 0) {
    alert('Selecciona al menos 1 pregunta HMW');
    return;
  }
  
  SCORM.setValue('cmi.suspend_data', JSON.stringify(state));
  renderPhase(3);
}

// FASE 3: IDEAR
function renderFase3(canvas) {
  canvas.innerHTML = `
    <div class="phase-card">
      <div class="phase-header">
        <div class="phase-number">◆ Fase 3 de 5</div>
        <h2 class="phase-title">Idear</h2>
        <p class="phase-description">Genera muchas ideas. Cantidad primero, calidad después.</p>
      </div>

      <div class="tool-section">
        <div class="tool-title">
          <div class="tool-icon">⚡</div>
          1. Crazy 8s
        </div>
        <p style="margin-bottom: 20px; color: var(--gray-dark);">Tienes 8 minutos para generar 8 ideas. Una idea por minuto. ¡No te detengas a pensar!</p>
        
        <div class="timer-display" id="crazy8-timer">08:00</div>
        
        <button class="btn-primary" id="btn-start-crazy8" onclick="startCrazy8()">Iniciar Timer ⏰</button>
        
        <div class="crazy8-grid" id="crazy8-grid" style="display: none;">
          ${Array(8).fill(0).map((_, i) => `
            <div class="crazy8-cell">
              <div class="crazy8-number">${i + 1}</div>
              <textarea id="idea-${i + 1}" placeholder="Idea ${i + 1}..."></textarea>
            </div>
          `).join('')}
        </div>
      </div>

      <div class="tool-section" id="scamper-section" style="display: none;">
        <div class="tool-title">
          <div class="tool-icon">🔄</div>
          2. SCAMPER - Variaciones con IA
        </div>
        <p style="margin-bottom: 20px; color: var(--gray-dark);">La IA aplicará la técnica SCAMPER a tus ideas para generar variaciones.</p>
        
        <button class="btn-primary" onclick="generateSCAMPER()">Generar Variaciones SCAMPER</button>
        
        <div id="scamper-results" class="hidden"></div>
      </div>

      <div id="thinking-3" class="thinking hidden"></div>

      <div style="margin-top: 50px; text-align: right;">
        <button class="btn-secondary" onclick="renderPhase(2)">← Volver a Fase 2</button>
        <button class="btn-primary" id="btn-fase3" onclick="completarFase3()" disabled>Continuar a Fase 4: Prototipar →</button>
      </div>
    </div>
  `;
}

let crazy8Timer = null;
let crazy8TimeLeft = 480; // 8 minutes in seconds

function startCrazy8() {
  document.getElementById('btn-start-crazy8').style.display = 'none';
  document.getElementById('crazy8-grid').style.display = 'grid';
  
  crazy8Timer = setInterval(() => {
    crazy8TimeLeft--;
    const mins = Math.floor(crazy8TimeLeft / 60).toString().padStart(2, '0');
    const secs = (crazy8TimeLeft % 60).toString().padStart(2, '0');
    document.getElementById('crazy8-timer').textContent = `${mins}:${secs}`;
    
    if (crazy8TimeLeft <= 0) {
      clearInterval(crazy8Timer);
      document.getElementById('crazy8-timer').textContent = '¡TIEMPO!';
      document.getElementById('crazy8-timer').style.color = 'var(--orange)';
      document.getElementById('scamper-section').style.display = 'block';
    }
  }, 1000);
}

async function generateSCAMPER() {
  // Recoger ideas de Crazy 8s
  const ideas = [];
  for (let i = 1; i <= 8; i++) {
    const idea = document.getElementById(`idea-${i}`).value.trim();
    if (idea) ideas.push(idea);
  }
  
  if (ideas.length === 0) {
    alert('Escribe al menos una idea en Crazy 8s');
    return;
  }
  
  state.data.crazy8Ideas = ideas;
  
  showThinking('thinking-3', 'La IA está aplicando SCAMPER a tus ideas...');

  const prompt = `Aplica la técnica SCAMPER a estas ideas para generar variaciones creativas:

IDEAS ORIGINALES:
${ideas.map((idea, i) => `${i + 1}. ${idea}`).join('\n')}

SCAMPER significa:
- Sustituir
- Combinar
- Adaptar
- Modificar/Magnificar/Minimizar
- Proponer otros usos
- Eliminar
- Reordenar/Reorganizar

Para cada idea original, genera 2-3 variaciones aplicando diferentes operaciones SCAMPER. Explica qué operación usaste.

Formato:
IDEA ORIGINAL 1: [idea]
→ Variación (Combinar): [nueva idea combinando elementos]
→ Variación (Eliminar): [nueva idea eliminando algo]
...`;

  try {
    const result = await llamarClaude(prompt);
    state.data.scamperIdeas = result;
    
    document.getElementById('scamper-results').innerHTML = `
      <div class="ai-response">
        <h4>🔄 Variaciones SCAMPER:</h4>
        ${result}
      </div>
      <div style="margin-top: 30px;">
        <h3 style="margin-bottom: 16px;">Selecciona tus 3 mejores ideas para prototipar:</h3>
        <div class="input-group">
          <label class="input-label">Idea 1:</label>
          <input type="text" id="selected-idea-1" class="input-field" placeholder="Copia aquí tu idea favorita">
        </div>
        <div class="input-group">
          <label class="input-label">Idea 2:</label>
          <input type="text" id="selected-idea-2" class="input-field" placeholder="Segunda mejor idea">
        </div>
        <div class="input-group">
          <label class="input-label">Idea 3:</label>
          <input type="text" id="selected-idea-3" class="input-field" placeholder="Tercera idea">
        </div>
      </div>
    `;
    document.getElementById('scamper-results').classList.remove('hidden');
    document.getElementById('btn-fase3').disabled = false;
    hideThinking('thinking-3');
  } catch (error) {
    hideThinking('thinking-3');
    alert('Error: ' + error.message);
  }
}

function completarFase3() {
  const idea1 = document.getElementById('selected-idea-1').value.trim();
  const idea2 = document.getElementById('selected-idea-2').value.trim();
  const idea3 = document.getElementById('selected-idea-3').value.trim();
  
  if (!idea1 || !idea2 || !idea3) {
    alert('Selecciona 3 ideas');
    return;
  }
  
  state.data.selectedIdeas = [idea1, idea2, idea3];
  SCORM.setValue('cmi.suspend_data', JSON.stringify(state));
  renderPhase(4);
}

// FASE 4: PROTOTIPAR
function renderFase4(canvas) {
  canvas.innerHTML = `
    <div class="phase-card">
      <div class="phase-header">
        <div class="phase-number">◆ Fase 4 de 5</div>
        <h2 class="phase-title">Prototipar</h2>
        <p class="phase-description">Haz tangible tu idea para poder testarla.</p>
      </div>

      <div class="info-box">
        <strong>Tus 3 ideas seleccionadas:</strong><br>
        1. ${state.data.selectedIdeas[0]}<br>
        2. ${state.data.selectedIdeas[1]}<br>
        3. ${state.data.selectedIdeas[2]}
      </div>

      <div class="input-group">
        <label class="input-label">¿Cuál idea vas a prototipar?</label>
        <select id="idea-to-prototype" class="input-field">
          <option value="0">Idea 1: ${state.data.selectedIdeas[0].substring(0, 50)}...</option>
          <option value="1">Idea 2: ${state.data.selectedIdeas[1].substring(0, 50)}...</option>
          <option value="2">Idea 3: ${state.data.selectedIdeas[2].substring(0, 50)}...</option>
        </select>
      </div>

      <div class="tool-section">
        <div class="tool-title">
          <div class="tool-icon">📖</div>
          1. Storyboard (6 viñetas)
        </div>
        <p style="margin-bottom: 20px; color: var(--gray-dark);">Cuenta la historia de cómo el usuario descubre, usa y se beneficia de tu solución.</p>
        
        <div class="storyboard-grid">
          ${['Descubrimiento', 'Primer uso', 'Experiencia clave', 'Beneficio obtenido', 'Compartir/recomendar', 'Resultado final'].map((label, i) => `
            <div class="storyboard-frame">
              <div class="storyboard-label">${i + 1}. ${label}</div>
              <textarea id="story-${i}" placeholder="Describe qué pasa en esta viñeta..."></textarea>
            </div>
          `).join('')}
        </div>
      </div>

      <div class="tool-section">
        <div class="tool-title">
          <div class="tool-icon">🛠️</div>
          2. Descripción del Prototipo
        </div>
        <p style="margin-bottom: 20px; color: var(--gray-dark);">Describe tu prototipo en detalle. ¿Cómo funciona? ¿Qué características tiene?</p>
        
        <textarea id="prototype-desc" class="input-field" style="min-height: 200px;" placeholder="Describe tu prototipo: interfaz, flujo de usuario, características clave, tecnología si aplica..."></textarea>
        
        <button class="btn-primary" onclick="generateValueProp()">Generar Value Proposition Canvas con IA</button>
      </div>

      <div id="value-prop-results" class="hidden"></div>

      <div id="thinking-4" class="thinking hidden"></div>

      <div style="margin-top: 50px; text-align: right;">
        <button class="btn-secondary" onclick="renderPhase(3)">← Volver a Fase 3</button>
        <button class="btn-primary" id="btn-fase4" onclick="completarFase4()" disabled>Continuar a Fase 5: Testear →</button>
      </div>
    </div>
  `;
}

async function generateValueProp() {
  const prototypeDesc = document.getElementById('prototype-desc').value.trim();
  if (!prototypeDesc) {
    alert('Describe tu prototipo primero');
    return;
  }
  
  const ideaIndex = document.getElementById('idea-to-prototype').value;
  const selectedIdea = state.data.selectedIdeas[ideaIndex];
  
  // Recoger storyboard
  const storyboard = [];
  for (let i = 0; i < 6; i++) {
    storyboard.push(document.getElementById(`story-${i}`).value);
  }
  state.data.storyboard = storyboard;
  state.data.prototypeDesc = prototypeDesc;
  
  showThinking('thinking-4', 'La IA está generando tu Value Proposition Canvas...');

  const prompt = `Genera un Value Proposition Canvas para esta solución:

IDEA: ${selectedIdea}
PROTOTIPO: ${prototypeDesc}
CONTEXTO: ${CASES[state.case].context}
USUARIO: ${CASES[state.case].persona}

Value Proposition Canvas tiene 2 lados:

PERFIL DEL CLIENTE:
- Trabajos del cliente (qué intenta hacer)
- Dolores (frustraciones, obstáculos)
- Ganancias (beneficios que busca)

MAPA DE VALOR:
- Productos y servicios (qué ofreces)
- Aliviadores de dolores (cómo reduces frustraciones)
- Creadores de ganancias (cómo generas beneficios)

Genera ambos lados Y evalúa el FIT entre ellos (0-100). ¿Qué tan bien tu propuesta resuelve las necesidades del cliente?

Formato claro y visual.`;

  try {
    const result = await llamarClaude(prompt);
    state.data.valueProposition = result;
    
    document.getElementById('value-prop-results').innerHTML = `
      <div class="ai-response">
        <h4>💎 Value Proposition Canvas:</h4>
        ${result}
      </div>
    `;
    document.getElementById('value-prop-results').classList.remove('hidden');
    document.getElementById('btn-fase4').disabled = false;
    hideThinking('thinking-4');
  } catch (error) {
    hideThinking('thinking-4');
    alert('Error: ' + error.message);
  }
}

function completarFase4() {
  SCORM.setValue('cmi.suspend_data', JSON.stringify(state));
  renderPhase(5);
}

// FASE 5: TESTEAR
function renderFase5(canvas) {
  canvas.innerHTML = `
    <div class="phase-card">
      <div class="phase-header">
        <div class="phase-number">◆ Fase 5 de 5</div>
        <h2 class="phase-title">Testear</h2>
        <p class="phase-description">Valida tus asunciones con feedback real (simulado por IA).</p>
      </div>

      <div class="info-box">
        <strong>Tu prototipo:</strong> ${state.data.prototypeDesc.substring(0, 200)}...
      </div>

      <div class="tool-section">
        <div class="tool-title">
          <div class="tool-icon">🎭</div>
          Test con 3 Usuarios Simulados
        </div>
        <p style="margin-bottom: 20px; color: var(--gray-dark);">La IA simulará 3 perfiles de usuario diferentes que darán feedback sobre tu prototipo.</p>
        
        <button class="btn-primary" onclick="runUserTests()">Ejecutar Tests de Usuario</button>
      </div>

      <div id="test-results" class="hidden"></div>

      <div class="tool-section" id="decision-section" style="display: none;">
        <div class="tool-title">
          <div class="tool-icon">🎯</div>
          Decisión: ¿Qué sigue?
        </div>
        <p style="margin-bottom: 20px; color: var(--gray-dark);">Basándote en el feedback, decide tu siguiente paso.</p>
        
        <div class="input-group">
          <label class="input-label">Tu decisión:</label>
          <select id="decision" class="input-field">
            <option value="">-- Selecciona --</option>
            <option value="iterate">ITERAR: Mejorar el concepto actual con los aprendizajes</option>
            <option value="pivot">PIVOTAR: Cambiar la dirección basándome en insights</option>
            <option value="persevere">PERSEVERAR: Seguir adelante, el concepto funciona</option>
          </select>
        </div>

        <div class="input-group">
          <label class="input-label">Plan de acción (próximos pasos concretos):</label>
          <textarea id="iteration-plan" class="input-field" style="min-height: 150px;" placeholder="¿Qué vas a hacer a continuación? Sé específico..."></textarea>
        </div>

        <button class="btn-primary" onclick="completarBootcamp()">Finalizar Bootcamp y Ver Resultados</button>
      </div>

      <div id="thinking-5" class="thinking hidden"></div>

      <div style="margin-top: 50px; text-align: right;">
        <button class="btn-secondary" onclick="renderPhase(4)">← Volver a Fase 4</button>
      </div>
    </div>
  `;
}

async function runUserTests() {
  showThinking('thinking-5', 'La IA está simulando feedback de 3 usuarios...');

  const prompt = `Simula feedback de 3 usuarios diferentes sobre este prototipo:

PROTOTIPO: ${state.data.prototypeDesc}
STORYBOARD: ${state.data.storyboard.join(' → ')}
CONTEXTO: ${CASES[state.case].context}

Simula 3 perfiles:
1. Early Adopter Entusiasta (positivo, da ideas)
2. Escéptico Pragmático (crítico constructivo, señala riesgos)
3. Usuario Edge Case (casos extremos, necesidades especiales)

Para cada usuario da:
- Nombre y perfil breve
- Lo que MÁS le gusta
- Lo que MÁS le preocupa
- 2-3 preguntas o sugerencias específicas

Luego da un RESUMEN de patrones comunes en el feedback.

Sé realista y específico.`;

  try {
    const result = await llamarClaude(prompt);
    state.data.feedbackResults = result;
    
    document.getElementById('test-results').innerHTML = `
      <div class="ai-response">
        <h4>👥 Feedback de Usuarios:</h4>
        ${result}
      </div>
    `;
    document.getElementById('test-results').classList.remove('hidden');
    document.getElementById('decision-section').style.display = 'block';
    hideThinking('thinking-5');
  } catch (error) {
    hideThinking('thinking-5');
    alert('Error: ' + error.message);
  }
}

async function completarBootcamp() {
  const decision = document.getElementById('decision').value;
  const plan = document.getElementById('iteration-plan').value.trim();
  
  if (!decision || !plan) {
    alert('Completa tu decisión y plan de acción');
    return;
  }
  
  state.data.decision = decision;
  state.data.iterationPlan = plan;
  
  // Calcular scores finales
  showThinking('thinking-5', 'Calculando tus métricas finales...');

  const prompt = `Evalúa este proyecto completo de Design Thinking y da puntuaciones 0-100:

FASE 1 - EMPATÍA:
Mapa: ${JSON.stringify(state.data.empathyMap)}
Entrevista: ${state.data.interviewQA}

FASE 2 - DEFINICIÓN:
POV: ${state.data.povStatement}
HMW: ${state.data.hmwQuestions.join(', ')}

FASE 3 - IDEACIÓN:
Ideas: ${state.data.crazy8Ideas.join(', ')}
Seleccionadas: ${state.data.selectedIdeas.join(', ')}

FASE 4 - PROTOTIPO:
${state.data.prototypeDesc}
Value Prop: ${state.data.valueProposition}

FASE 5 - TESTING:
Feedback: ${state.data.feedbackResults}
Decisión: ${decision}
Plan: ${plan}

Da puntuaciones:
1. PROFUNDIDAD DE EMPATÍA (0-100): ¿Cuán profundo fue el entendimiento del usuario?
2. CREATIVIDAD (0-100): ¿Cuán originales son las ideas?
3. VIABILIDAD (0-100): ¿Cuán viable es la solución propuesta?
4. CALIDAD DE PROTOTIPO (0-100): ¿Cuán bien está definido el prototipo?
5. OVERALL (promedio)

Formato:
EMPATHY: X/100
CREATIVITY: Y/100
VIABILITY: Z/100
PROTOTYPE: W/100
OVERALL: V/100

FEEDBACK FINAL:
[2-3 párrafos de feedback constructivo sobre todo el proceso]`;

  try {
    const result = await llamarClaude(prompt);
    
    // Extraer scores
    const empathyMatch = result.match(/EMPATHY:\s*(\d+)/);
    const creativityMatch = result.match(/CREATIVITY:\s*(\d+)/);
    const viabilityMatch = result.match(/VIABILITY:\s*(\d+)/);
    const prototypeMatch = result.match(/PROTOTYPE:\s*(\d+)/);
    const overallMatch = result.match(/OVERALL:\s*(\d+)/);
    
    state.scores = {
      empathy: empathyMatch ? parseInt(empathyMatch[1]) : 0,
      creativity: creativityMatch ? parseInt(creativityMatch[1]) : 0,
      viability: viabilityMatch ? parseInt(viabilityMatch[1]) : 0,
      prototype: prototypeMatch ? parseInt(prototypeMatch[1]) : 0,
      overall: overallMatch ? parseInt(overallMatch[1]) : 0
    };
    
    SCORM.setScore(state.scores.overall);
    SCORM.setCompleted();
    SCORM.commit();
    
    hideThinking('thinking-5');
    mostrarResultadosFinales(result);
  } catch (error) {
    hideThinking('thinking-5');
    alert('Error: ' + error.message);
  }
}

function mostrarResultadosFinales(feedback) {
  const canvas = document.getElementById('canvas-content');
  canvas.innerHTML = `
    <div class="phase-card">
      <div class="phase-header">
        <div class="phase-number">🎉 ¡BOOTCAMP COMPLETADO!</div>
        <h2 class="phase-title">Tus Resultados</h2>
        <p class="phase-description">Has completado el ciclo completo de Design Thinking.</p>
      </div>

      <div class="final-dashboard">
        <div class="metric-card">
          <div class="metric-value">${state.scores.empathy}</div>
          <div class="metric-label">Empatía</div>
        </div>
        <div class="metric-card">
          <div class="metric-value">${state.scores.creativity}</div>
          <div class="metric-label">Creatividad</div>
        </div>
        <div class="metric-card">
          <div class="metric-value">${state.scores.viability}</div>
          <div class="metric-label">Viabilidad</div>
        </div>
        <div class="metric-card">
          <div class="metric-value">${state.scores.overall}</div>
          <div class="metric-label">Global</div>
        </div>
      </div>

      <div class="ai-response">
        <h4>📊 Feedback Final de la IA:</h4>
        ${feedback}
      </div>

      <div style="margin-top: 40px; text-align: center;">
        <h3 style="margin-bottom: 20px;">¿Quieres guardar tu proyecto?</h3>
        <div class="input-group" style="max-width: 500px; margin: 0 auto;">
          <label class="input-label">Tu email:</label>
          <input type="email" id="final-email" class="input-field" placeholder="tu@email.com">
        </div>
        <button class="btn-primary" onclick="enviarProyecto()">Enviar Proyecto Completo por Email</button>
        <button class="btn-secondary" onclick="location.reload()">Nuevo Bootcamp</button>
      </div>

      <div id="email-success" class="hidden" style="margin-top: 20px; text-align: center; color: var(--green); font-weight: 700;"></div>
    </div>
  `;
}

async function enviarProyecto() {
  const email = document.getElementById('final-email').value.trim();
  if (!email || !email.includes('@')) {
    alert('Introduce un email válido');
    return;
  }

  const contenido = `
DESIGN THINKING BOOTCAMP - PROYECTO COMPLETO
============================================

RETO: ${CASES[state.case].name}
${CASES[state.case].context}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FASE 1: EMPATIZAR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MAPA DE EMPATÍA:
${JSON.stringify(state.data.empathyMap, null, 2)}

ENTREVISTA:
${state.data.interviewQA}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FASE 2: DEFINIR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

POV STATEMENT:
${state.data.povStatement}

HMW QUESTIONS:
${state.data.hmwQuestions.map((q, i) => `${i + 1}. ${q}`).join('\n')}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FASE 3: IDEAR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CRAZY 8s IDEAS:
${state.data.crazy8Ideas.map((idea, i) => `${i + 1}. ${idea}`).join('\n')}

SCAMPER:
${state.data.scamperIdeas}

IDEAS SELECCIONADAS:
${state.data.selectedIdeas.map((idea, i) => `${i + 1}. ${idea}`).join('\n')}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FASE 4: PROTOTIPAR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STORYBOARD:
${state.data.storyboard.map((frame, i) => `${i + 1}. ${frame}`).join('\n')}

DESCRIPCIÓN PROTOTIPO:
${state.data.prototypeDesc}

VALUE PROPOSITION:
${state.data.valueProposition}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FASE 5: TESTEAR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FEEDBACK USUARIOS:
${state.data.feedbackResults}

DECISIÓN: ${state.data.decision}

PLAN DE ACCIÓN:
${state.data.iterationPlan}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PUNTUACIONES FINALES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Empatía: ${state.scores.empathy}/100
Creatividad: ${state.scores.creativity}/100
Viabilidad: ${state.scores.viability}/100
Prototipo: ${state.scores.prototype}/100
OVERALL: ${state.scores.overall}/100

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SimulabAI | Universidad de Salamanca
Prof. Jorge Matías-Pereda
Powered by Claude Sonnet 4
  `.trim();

  try {
    const formData = new FormData();
    formData.append('email', email);
    formData.append('asunto', `Design Thinking Bootcamp: ${CASES[state.case].name}`);
    formData.append('mensaje', contenido);

    const response = await fetch('https://formsubmit.co/ajax/' + email, {
      method: 'POST',
      body: formData
    });

    if (response.ok) {
      document.getElementById('email-success').textContent = '✅ Proyecto enviado. Revisa tu email.';
      document.getElementById('email-success').classList.remove('hidden');
      document.getElementById('final-email').disabled = true;
    }
  } catch (error) {
    alert('Error al enviar. Copia manualmente el contenido.');
  }
}

// UTILITY FUNCTIONS
function showThinking(id, text) {
  const el = document.getElementById(id);
  if (el) {
    el.innerHTML = `
      <div class="thinking-spinner"></div>
      <div class="thinking-text">${text}</div>
    `;
    el.classList.remove('hidden');
    el.classList.add('visible');
  }
}

function hideThinking(id) {
  const el = document.getElementById(id);
  if (el) {
    el.classList.remove('visible');
    setTimeout(() => el.classList.add('hidden'), 300);
  }
}
</script>

</body>
</html>
